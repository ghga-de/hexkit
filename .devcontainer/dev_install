#!/bin/bash
# install hexkit in dev container

cd /workspace

# install in edit mode with testing dependencies
pip install \
    -e \
    ".[all,dev]"

# install pre-commit hooks to git:
pre-commit install
